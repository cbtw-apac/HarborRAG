runtime:
  operation_timeout_seconds: 180.0
  llm_operation_cost_usd: "0.01"
  derived:
    enabled: true
    parent_max_output_tokens: 512
    parent_max_calls: 256

tenants:
  - tenant_id: DEFAULT
    mode: llm  
    budget:
      max_concurrency: 2
      daily_token_cap: 200000000
      daily_cost_usd: "15.00"
    sources:
      - source_scope_id: scope-harborrag-on-topology-real
