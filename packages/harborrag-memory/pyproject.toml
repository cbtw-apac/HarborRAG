[project]
name = "harborrag-memory"
version = "0.1.0"
description = "Memory for HarborRAG."
requires-python = ">=3.12"
readme = "README.md"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/harborrag_memory"]
