version: 1

# Keys are the names you pass to `harborrag ingest run NAME`. URLs and credentials are
# read from the environment variables named here (see .env), never written into YAML.
# Add more connections from config/connectors.example.yaml in the HarborRAG repository.
connectors:
@{connector_blocks}
